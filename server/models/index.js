const User = require('./User');
const Game = require('./Game');
const Session = require('./Session');
const Record = require('./Record');

// now all models are connected properly
module.exports = { User, Game, Session, Record };
