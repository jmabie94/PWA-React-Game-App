// const { useState } = require('react');

// export const Room = (props, { key, context, initiator, clientProps }) => {
//     const [messages, setMessages] = useState([]);
//     const [clients, setClients] = useState([]);

//     useClientEffect(() => {
//         const user = tryGetUser(context);
//         const client = {
//             user,
//             id: context.headers['x-unique-id'],
//         };

//         if (initiator !== )
//     })
// }

// this all seems like it only works with @state-less/react-server dependency, which seems to be super new and in beta, not sure if we should use for this project
